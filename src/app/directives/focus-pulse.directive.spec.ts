import { FocusPulseDirective } from './focus-pulse.directive';

describe('FocusPulseDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusPulseDirective();
    expect(directive).toBeTruthy();
  });
});
